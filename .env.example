# Base URL of the backend API. Default "/api" works with the dev-server proxy
# (vite.config.ts) and the production nginx proxy. Set an absolute URL only
# when the API is served from a different host.
VITE_API_URL=/api
